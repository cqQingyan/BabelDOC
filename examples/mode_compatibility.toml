[babeldoc]
# 常用：高兼容模式，遇到排版或渲染问题时启用。
mode = "compatibility"
lang-in = "en"
lang-out = "zh"
files = ["./sample.pdf"]
openai = true
openai-api-key = "YOUR_API_KEY"
openai-model = "gpt-4o-mini"
output = "./output"
qps = 3
# 可选：与高兼容模式搭配的其他安全项
use-alternating-pages-dual = true
